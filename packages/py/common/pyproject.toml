[project]
name = "dty-common"
version = "0.1.0"
description = "Shared Python utilities for the monorepo"
requires-python = ">=3.12,<3.13"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dty_common"]
